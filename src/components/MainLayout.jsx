import React from 'react'

const MainLayout = ({ children }) => {
    return (
        <div className='my-layout'>
            {children}
        </div>

    )
};

export default MainLayout;